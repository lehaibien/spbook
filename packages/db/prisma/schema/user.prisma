model Role {
    id String @id @default(uuid(7)) @db.Uuid
    name String @unique
    displayName String

    users User[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@map("role")
}